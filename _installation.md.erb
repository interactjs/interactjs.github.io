Installation
============

Download
--------

```shell
$ bower install interact
$ npm install interact.js
```

Either:

* with [Bower](http://bower.io/)
* with [npm](https://www.npmjs.org/)
* manually from http://interactjs.io/#download

Link
----

```html
<script src="/path/to/interact.js"></script>
```

After downloading the file, add a `<script>` tag who's `src` attribute points
to the file to your page's HTML.

jsDelivr CDN
------------

```html
<script src="//cdn.jsdelivr.net/interact.js/VERSION/interact.min.js"></script>
```

If you'd prefer to use the [jsDelivr
CDN](http://www.jsdelivr.com/#!interact.js) instead, simply add a script tag
pointing to `//cdn.jsdelivr.net/interact.js/VERSION/interact.min.js`. `VERSION`
should be replaced by the version number that you want to use.

Module
------

`interact` is exposed as a CommonJS module, an AMD module, or a global object
depending on what the environment supports.

